# Lab credentials and configuration overrides.
# Copy to .env and adjust as needed. Never commit .env with real secrets.

# Intentionally weak lab credentials (do not reuse outside the lab)
DB_PASSWORD=SuperSecret1!
DB_USER=app
DB_NAME=appdb

# Flag overrides (optional)
FLAG_01=FLAG{enum_the_web}
FLAG_02=FLAG{lateral_move_success}
FLAG_03=FLAG{db_creds_found}
FLAG_04=FLAG{root_privesc_complete}

# Internal service token
SECRET_KEY=internal_lab_secret_do_not_reuse
